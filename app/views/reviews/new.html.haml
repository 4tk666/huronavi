.review
  = form_with model: @review, url:reviews_path,local: true do |f|
    = f.label :title
    = f.text_field :title
    = f.label :text
    = f.text_field :text
    = f.fields_for :photos do |img|
      = img.file_field :image,type:"file"
    = f.submit